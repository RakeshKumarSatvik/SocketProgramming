
The Server D is up and running.

The Server D has the following neighbor information:

Neighbor-------Cost
serverB		15

The Server D finishes sending its neighbor information to the client with TCP port number 25251 and IP address 127.0.0.1 (Client's TCP port number and IP address).

For this connection with the Client, the Server D has TCP port number 56161 and IP address 127.0.0.1.

The Server D has received the network topology from the Client with UDP port number 56174 and IP address 127.0.0.1 (Client's UDP port number and IP address) as follows:
Edge----Cost
AB		15
BB		30
CC		30
CD		15

For this connection with Client, The Server D has UDP port number 24251 and IP address 127.0.0.1.
